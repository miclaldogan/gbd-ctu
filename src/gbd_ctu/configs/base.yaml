project:
  name: GBD-CTU
  seed: 42

paths:
  data_root: data/ctu13
  graph_dir: artifacts/graphs
  checkpoint_dir: artifacts/checkpoints
  report_dir: artifacts/reports

training:
  epochs: 20
  batch_size: 4
  learning_rate: 0.001
  weight_decay: 0.0001
  focal_gamma: 2.0
  focal_alpha: 0.75
  wandb_project: null
