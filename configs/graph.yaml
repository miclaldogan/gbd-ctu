graph:
  train_size: 0.6
  val_size: 0.2
  seed: 42
  # flow-level graph: nodes = flows (22-dim features), edges = shared IP
  node_feature_dim: 22
  edge_feature_dim: 0
  max_degree: 50
  self_loops: false
  # ip-level legacy settings
  min_flows_per_node: 1
  undirected_option: false
